html
  head
    title Hello!
  body
      h1 Product List:

      ul
      each product in products
        li= product.name

      form(action='/products', method='POST')
        p Add a new product!
        input(name='product_name')
        input(type='submit')
